---
order: 2
layout: '@docsLayout'
title: 'signalizejs/is-visible'
description: 'Get info if element is visible.'
---
import ImportMapDependencies from '@components/DocsImportmapDependencies.astro';

## Installation
<ImportMapDependencies module="is-visible" />

```js
const { isVisible } = await signalize.resolve('is-visible');
```

## API

### isVisible
Check if element is visible. Method returns `true` or `false`.
```js
isVisible(document.querySelector('#element'));
```
