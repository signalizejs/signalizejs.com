---
order: 2
layout: '@docsLayout'
title: 'signalizejs/off'
description: 'Get element coordinates.'
---

Off plugin is included within the Signalize core, so you don't have to import it.

## Installation

Add a public path for `signalizejs/offset` into your import map.

Then you can resolve it.

```js
const { offset } = await signalize.resolve('offset');
```

## API

### offset
Get element coordinates.
```js
const { top, left, right, bottom } = offset(document.querySelector('#element'));
```
